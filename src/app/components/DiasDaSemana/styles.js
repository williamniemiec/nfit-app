import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    area:{
        flexDirection:'row',
        flexWrap:'wrap',
        justifyContent:"center",
        alignItems:'center'
    }
})